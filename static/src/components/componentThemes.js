// import all css component files into here
// this file exists to decouple the components' css files' hook names from .js files
import btn from './Button.module.css';

export const ButtonThemes = {
    // primary is purple
    default_primary: btn.default_primary,
    
}